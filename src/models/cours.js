export default class Cours {
    constructor(id, titre, description,nomduprof,image,contenu) {
      this.id = id;
      this.titre =titre;
      this.description = description;
      this.nomduprof = nomduprof;
      this.image = image;
      this.contenu = contenu;
    }
  } 